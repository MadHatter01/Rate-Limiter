<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Config Portal</title>
    <style>
        body {
            background: #121212;
            color: #eae4cc;
            font-family: system-ui, -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, 'Open Sans', 'Helvetica Neue', sans-serif;
            display: flex;
            justify-content: center;
            align-items: center;
            margin: 0;
            min-height: 100vh;
        }

        .container {
            background: #1e1e1e;
            border: 1rem;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.3);
            padding: 2rem;
            max-width: 400px;
            width: 100%;

        }

        h1 {
            font-size: 1.8rem;
            margin-bottom: 1rem;
            color: #ffdd57;
            text-align: center;
        }

        .current-config {
            background: rgba(255, 255, 255, 0.1);
            padding: 1rem;
            border-radius: 8px;
            margin-bottom: 1.5rem;
        }

        .current-config p {
            font-size: 1.1rem;
            margin: 0.5rem 0;
        }

        .current-config span {
            text-transform: uppercase;
            font-size: 0.7rem;
            letter-spacing: 0.05rem;
            font-weight: 700;
        }

        .update-config form {
            display: flex;
            flex-direction: column;
            gap:1rem;

        }

        label{
            font-weight: bold;
            text-align: left;
            display: block;
        }
        input {
            width: 100%;
            padding: 0.7rem;
            border: 1px solid #555;
            border-radius: 6px;
            font-size: 1rem;
            background: #222;
            color: #eae4cc;
            outline: none;
            transition: border-color 0.3s;
        }

        input:focus {
            border-color: #ffdd57;
        }

        button {
            padding: 0.7rem;
            font-size: 1.1rem;
            font-weight: bold;
            border: none;
            border-radius: 6px;
            background: #ffdd57;
            color: #121212;
            cursor: pointer;
            transition: background 0.3s ease;
        }

        button:hover {
            background: #f3c100;
        }

    </style>
</head>

<body>

    <div class="container">
        <h1>Config Portal</h1>

        <section class="current-config">
            <span>Current</span>
            <p><strong>Bucket Size : </strong> <%= config.bucketSize %>
            </p>
            <p><strong>Refill Rate : </strong> <%= config.refillRate %>
            </p>
        </section>
        <section class="update-config">
            <form action="/config" method="POST">
                <label for="bucketSize">Bucket Size</label>
                <input type="number" name="bucketSize" id="bucketSize" value="<%= config.bucketSize%>" required />
                <label for="refillRate">Refill Rate</label>
                <input type="number" name="refillRate" id="refillRate" value="<%= config.refillRate%>" required />
                <button type="submit">Save Config</button>
            </form>
        </section>
    </div>
</body>

</html>